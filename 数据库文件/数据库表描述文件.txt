product（产品表）
id，
name（产品名称），
icon（产品小图标地址），
specifications（产品规格选项），
swiper（产品轮播图），
detailed（产品其它信息），
text（产品描述）
----------------------------
specifications（产品“规格之间搭配”的价格及库存表）
id，
product_id（产品id），
name（产品规格搭配的值）,
price（这个规格搭配的价格），
number（这个规格搭配的库存数量）
-----------------------------
user（用户表）
id,
account（用户账号）,
password（用户密码）
-----------------------------
product_order（订单表）
id,
user_id（用户id），
create_time（订单创建时间），
product_list（这个订单购买的产品）,
price（订单总价）,
status（订单状态）